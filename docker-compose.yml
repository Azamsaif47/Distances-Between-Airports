version: '3.4'

services:
  distanceapi:
    image: airport-distance-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
    volumes:
      - .data/Microsoft/UserSecrets:/root/.microsoft/usersecrets:ro
      - .data/ASP.NET/Https:/root/.aspnet/https:ro